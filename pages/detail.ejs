<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link href="/main.css" rel="stylesheet" />
  </head>
  <body class="grey-bg">
    <%- include('nav.ejs') %>
    <div class="detail-bg">
      <h4><%= result.title %></h4>
      <p><%= result.content %></p>
      <a href="/chat/request?writerId=<%= result.user %>">채팅하기</a>

      <hr style="margin-top: 60px" />
      <div>
        <p><strong>작성자</strong> 댓글내용</p>
      </div>
      <% for (let i = 0; i < comment.length; i++) { %>
      <p><strong><%= comment[i].writer %></strong> <%= comment[i].content %></p>
      <% } %>
      <form action="/comment" method="POST">
        <input name="content" />
        <input
          name="parentId"
          value="<%= result._id %>"
          style="display: none"
        />
        <button type="submit">댓글작성</button>
      </form>
    </div>
  </body>
</html>
